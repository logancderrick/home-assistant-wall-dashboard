JavaScript in this folder is copied from the **voice-satellite-card-integration** project (MIT):

https://github.com/jxlarrea/voice-satellite-card-integration

Files: `custom-model-runner.js`, `micro-inference.js`, `micro-models.js` (patched `modelsBase()` for SkyDark), `tflite-quant-reader.js`, `micro-frontend-js/index.js`.

Wake-word `.tflite` binaries in `frontend/public/voice-models/` are from the same repo under `custom_components/voice_satellite/models/`.
